export type ClassAdd = {
    teacher_id: string;
    subject_id: string;
}

export type ClassUpdate = {
    teacher_id: string;
    subject_id: string;
}

export type ClassAdministered = {
    teacher_id: string;
    subject_id: string;
}

export type ClassRemove = {
    teacher_id: string;
    subject_id: string;
}
